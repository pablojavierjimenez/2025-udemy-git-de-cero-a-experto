name: First Workflow
on: 
  push:
    branches:
      - develop
jobs:
  job1: 
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Print Hello World
        run: echo "Hello, World!"